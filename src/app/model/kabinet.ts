export interface Kabinet {
    id?: number,
    name?: string,
    address?: string,
    website?: string,
    phoneNumber?: string,
    longitude?: number,
    latitude?: number,
    geometry?: string
}